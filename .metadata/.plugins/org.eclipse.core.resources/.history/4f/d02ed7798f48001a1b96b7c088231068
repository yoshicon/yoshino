package GUItes;

import java.awt.*;
import java.awt.event.*;
import java.io.*;

import javax.imageio.ImageIO;
import javax.swing.*;

import GUItes.Elsanna.mypan;

public class Grp extends JFrame implements ActionListener{
	
	JButton bt1 = new JButton("란-듐");
	JLayeredPane lap = new JLayeredPane();
	
	
	public Grp() {
		setTitle("엘산나");
		
		BorderLayout lay = new BorderLayout();
		setLayout(lay);
		
		bt1.addActionListener(new ActionListener() {

			@Override
			public void actionPerformed(ActionEvent e) {
				setM();

			}
		});
		
		lap.setToolTipText("머먹음?");;
		add(bt1, BorderLayout.SOUTH);
		add(lap, BorderLayout.CENTER);
		
		setDefaultCloseOperation(EXIT_ON_CLOSE);
		setSize(300, 300);
		setVisible(true);
	}
	
	public void setM() {
	}
	
	@Override
	public void actionPerformed(ActionEvent e) {
		// TODO Auto-generated method stub
		
	}

}
